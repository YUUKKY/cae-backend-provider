<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.demo.mapper.OrderDataMapper">

    <resultMap id="OrderDataResultMap" type="com.demo.model.OrderDataDo">
        <id column="id" property="id"/>
        <result column="updated_at" jdbcType="BIGINT" property="timestamp"/>
        <result column="quantity" jdbcType="INTEGER" property="quantity"/>
        <result column="status" jdbcType="VARCHAR" property="status"/>
        <result column="price" jdbcType="INTEGER" property="price"/>
    </resultMap>

    <select id="getAllOrders" resultMap="OrderDataResultMap">
        SELECT *
        FROM orders ORDER BY updated_at DESC
    </select>

    <insert id="insert">
        INSERT INTO orders(id, status, quantity, updated_at, price)
        VALUES (#{id}, #{status}, #{quantity}, #{timestamp}, #{price})
    </insert>
</mapper>
